# @gala-chain/test-e2e

E2E clients and token helpers for GalaChain. Depends on `@gala-chain/api` and `@gala-chain/client`.

Unit helpers (`fixture`, `writesMap`, matchers) live in `@gala-chain/test`.
